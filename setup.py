from setuptools import setup, find_namespace_packages

# Since version 30.3.0, Setuptools allows using configuration files (usually
# setup.cfg) to define a package's metadata and other options that are
# normally supplied to the setup() function (declarative config).
# see, https://setuptools.readthedocs.io/en/latest/userguide/declarative_config.html
setup(
    #packages=find_namespace_packages(where="src"),
)
